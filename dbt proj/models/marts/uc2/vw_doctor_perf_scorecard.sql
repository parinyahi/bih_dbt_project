{{ config(materialized='view', schema='GOLD') }}

select
  DOCTOR_ID,
  SPECIALTY_TYPE,
  SPECIALTY,
  TOTAL_CONSULTATIONS,
  UNIQUE_PATIENTS,
  ACTIVE_DAYS,
  UNIQUE_CONDITIONS_TREATED,
  WALK_IN_CONSULTS,
  SCHEDULED_CONSULTS,
  SUCCESSFUL_CONSULTS,
  PATIENT_NO_SHOWS,
  PENDING_CONSULTS,
  SUCCESS_RATE_PCT,
  NO_SHOW_RATE_PCT,
  AVG_CONSULTS_PER_DAY,
  REPEAT_PATIENT_RATIO,
  SERVICE_SPAN_DAYS,
  DOCTORS_IN_SPECIALTY,
  SPECIALTY_TOTAL_CONSULTS,
  SPECIALTY_UNIQUE_PATIENTS,
  AVG_SPECIALTY_SUCCESS_RATE_PCT,
  AVG_SPECIALTY_NO_SHOW_RATE_PCT,
  PERFORMANCE_TIER,
  WORKLOAD_LEVEL,
  FIRST_CONSULTATION,
  LAST_CONSULTATION,
  current_timestamp as LOAD_TIMESTAMP
from {{ ref('doctor_benchmarked') }}
